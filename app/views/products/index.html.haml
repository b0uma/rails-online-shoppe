.container
  %h1 Listing products
  = link_to 'Cart', '/cart'
  %h1 This page should be styled to be more user friendly instead of a tabular format
  = form_tag "/products", method: :get do
    = select_tag "category", options_from_collection_for_select(@categories, "id", "name", @selected_category.id)
    = submit_tag "Filter by category"
    = submit_tag "Reset"
  %table.table.table-bordered.table-striped.table-hover
    %thead
      %tr
        %th Name
        %th Price
        %th Description
    %tbody
      - @products.each do |product|
        %tr
          %td= link_to product.name, product_path(product)
          %td= number_to_currency(product.price)
          %td= product.description


